spring:
  datasource:
    url: jdbc:h2:mem:md_agent_test;MODE=MySQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  sql:
    init:
      mode: never
  ai:
    openai:
      base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
      api-key: ${QWEN_API_KEY:test-key}
      chat:
        options:
          model: qwen3.8-flash
